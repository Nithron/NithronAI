version: "3.9"
services:
  nai-core:
    build: ../apps/nai-core
    ports:
      - "8000:8000"
    volumes:
      - ../apps/nai-core/data:/app/data
    environment:
      - NAI_ALLOW_NETWORK=false
  # qdrant:
  #   image: qdrant/qdrant:latest
  #   ports: ["6333:6333", "6334:6334"]
  # ollama:
  #   image: ollama/ollama:latest
  #   ports: ["11434:11434"]
